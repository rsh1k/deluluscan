# TEMPLATE ONLY — do NOT commit real values. The ALB reads the Google OAuth
# client id/secret from this k8s Secret (name referenced by ingress.yaml's
# auth-idp-oidc.secretName). Create it out-of-band, e.g.:
#
#   kubectl -n deluluscan create secret generic deluluscan-dashboard-oidc \
#     --from-literal=clientId="<GOOGLE_OAUTH_CLIENT_ID>" \
#     --from-literal=clientSecret="<GOOGLE_OAUTH_CLIENT_SECRET>"
#
# The Google OAuth client (from the target Google Cloud / Workspace admin) must:
#   * have consent screen set to INTERNAL (restricts login to example.com accounts)
#   * list the ALB callback as an authorized redirect URI:
#       https://deluluscan.target.cloud/oauth2/idpresponse
apiVersion: v1
kind: Secret
metadata:
  name: deluluscan-dashboard-oidc
  namespace: deluluscan
type: Opaque
stringData:
  clientId: "REPLACE_ME"
  clientSecret: "REPLACE_ME"
